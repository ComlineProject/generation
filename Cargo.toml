[workspace]
resolver = "2"
members = [
    "lib-gen/_core",
    # Body port pending — de-rot G2, match current IR (still on `EnumVariant(_)`,
    # `Constant` without `span`):
    #   "lib-gen/lua"
    #   "lib-gen/luau"
    # Not started — `use comline::…` (pre-rename path), no wiring:
    #   "lib-gen/c"
    #   "lib-gen/python"
    #   "lib-gen/typescript"
]

# Local iteration against a sibling `core` checkout — uncomment to build
# `comline-core` from the working tree instead of the pinned git rev. Re-comment
# before pushing; CI has no sibling checkout.
# [patch."https://github.com/ComlineProject/core"]
# comline-core = { path = "../core/core" }

[profile.release]
opt-level = 2
