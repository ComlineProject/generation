// Relative Modules
//
// TODO(de-rot G2): the lib / dylib packaging layer (manifest + FFI wrapper +
// build glue). Both submodules target a pre-audit core API — revived in G2,
// see design/generation.md.
// pub mod rust;
// pub mod rust_c_ffi;
